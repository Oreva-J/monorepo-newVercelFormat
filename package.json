{
  "name": "my-monorepo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run index.ts",
    "build": "cd packages/client && bun run build",
    "preview": "cd packages/client && bun run preview"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vercel/node": "^5.5.5",
    "typescript": "^5.3.3"
  }
}